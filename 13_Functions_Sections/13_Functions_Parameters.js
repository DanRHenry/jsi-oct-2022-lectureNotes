/* 
!   Function Parameters

    - Placed within the parenthesis
    - Placeholders for the data that is passed through the functions.
    - When functions are called/invoked, these arguments are passed through them.
    - They are strictly used by the function's body
*/

    function hi(name) {
        console.log(`Hi, ${name}`);
    }

hi(`Bruce`)
console.log(`Hi, bruce`);